# Normalizar todo a LF adentro del repo.
* text=auto eol=lf

# Estos dos los ejecuta Windows y quiere CRLF si o si.
*.ps1 text eol=crlf
*.psm1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

# El .rar es binario: que git no lo toque nunca.
*.rar binary
